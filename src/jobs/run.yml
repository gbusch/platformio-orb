description: >
  Compiles the Platform IO project

executor: default

steps:
  - run:
      name: "Compile"
      command: platformio ci --build-dir="./bin" --keep-build-dir --project-conf=platformio.ini ./src/
